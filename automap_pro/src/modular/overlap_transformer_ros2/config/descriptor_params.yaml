# 本文件为 fallback；由 automap_pro launch 启动时，参数从 system_config.yaml 的
# loop_closure.overlap_transformer（及 range_image.height/width）读取并注入，此处可不填。
overlap_transformer_descriptor_server:
  ros__parameters:
    model_path: ""
    fov_up: 3.0
    fov_down: -25.0
    proj_H: 64
    proj_W: 900
    max_range: 80.0
