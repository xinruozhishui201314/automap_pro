Cursor Agent 模式中文提问模板库
使用说明：在 Cursor 的 Agent 模式（CMD+I 或对话框选择 Agent）中直接粘贴以下模板，填入 [方括号] 中的内容即可使用。Agent 模式下 Cursor 会自动读取文件、搜索代码、执行命令。

一、错误排查类
模板 1：通用 Bug 排查
text

请按照排障SOP流程来排查这个Bug。

## Bug 描述
- **实际表现**：[描述你看到的错误行为]
- **期望表现**：[描述正确的行为应该是什么]
- **错误信息/堆栈**（如有）：
[粘贴错误信息或堆栈信息]

text

- **出现位置**：[哪个页面/接口/功能/模块]
- **复现步骤**：
  1. [步骤1]
  2. [步骤2]
  3. [步骤3]
- **出现频率**：[每次都出现 / 偶发 / 特定条件下出现]
- **最近改动**：[是否有最近的代码改动可能相关，没有就写"不清楚"]

## 要求
1. 分析深度至少到 L4（跨模块层级），不要只看报错那一个文件
2. 不要停在第一个看似合理的解释上——至少提出3个假设并逐一验证
3. 每个结论都必须附上具体的代码证据（文件名+行号）
4. 用5-Why方法追溯到根本原因，不要只修表面症状
5. 最终告诉我：这是根因修复还是症状修复？
模板 2：运行时报错排查
text

请以"根因分析师"的角色来排查这个运行时错误。

## 错误信息
[粘贴完整的错误信息和堆栈追踪]

text


## 上下文
- 触发操作：[什么操作触发了这个错误]
- 运行环境：[开发环境/测试环境/生产环境]
- 首次出现：[什么时候开始出现的——某次部署后？随机出现？]
- 出现频率：[每次都报 / 偶尔报 / 只在特定数据下报]

## 我已经尝试过的排查
- [列出你已经做过的调试步骤]

## 分析要求
1. 先读完整的堆栈信息，找到里面提到的每一个文件
2. 从堆栈顶部（抛出错误的地方）开始，逐层追踪每个调用帧
3. 在每一层都要读实际代码，不要根据函数名猜测功能
4. 用5-Why方法追问根因，每一层都要附带代码证据
5. 判断这个根因是否可能在代码库的其他地方也存在
6. 区分清楚：你找到的是"症状"还是"原因"
模板 3：逻辑错误排查（没报错但结果不对）
text

请以"根因分析师"的角色来排查这个逻辑错误。
注意：这个Bug没有任何报错信息，但是行为不正确。

## 问题描述
- **实际行为**：[系统实际做了什么]
- **期望行为**：[系统应该做什么]
- **具体例子**：
  - 输入：[具体的输入数据或操作]
  - 期望输出：[你期望的结果]
  - 实际输出：[你得到的结果]

## 入口点
相关的用户流程从 [文件/组件/接口端点] 开始

## 分析要求
1. 追踪完整的数据流，从输入到输出：
   输入 → [处理步骤1] → [处理步骤2] → ... → 输出
2. 在每个步骤验证：
   - 什么数据到达了这一步？
   - 施加了什么转换？
   - 什么数据离开了这一步？
3. 找到实际数据与期望数据开始出现分歧的**精确位置**
4. 找到分歧点后，用5-Why分析理解为什么会有这个分歧
5. 检查代码库中是否有其他地方使用了相同的错误逻辑（复制粘贴Bug）
6. 在根因层面提出修复方案，不要在症状层面修补
模板 4：偶发性 Bug 排查
text

请以"根因分析师"的角色来排查这个偶发性Bug。
这个Bug很难稳定复现，需要重点排查并发和状态相关的问题。

## 症状
- **现象**：[Bug出现时的表现]
- **频率**：大约 [X] 次操作中出现 [Y] 次，或 [描述规律]
- **似乎会增加出现概率的条件**：[如有已知规律]
- **绝对不会出现的条件**：[如有已知]

## 怀疑区域
[如果你有怀疑的代码区域就写，否则写"不确定"]

## 分析要求
因为这是偶发性Bug，请重点排查以下常见根因：

### 1. 竞态条件 / 时序问题
- 搜索受影响区域的异步操作
- 查找被多个异步路径访问的共享状态
- 检查是否有遗漏的 await / Promise 处理
- 检查是否有事件顺序假设

### 2. 状态管理问题
- 查找过期闭包（stale closures）
- 查找应该是不可变更新但实际是直接修改的状态
- 检查操作之间是否缺少状态重置
- 检查全局/共享状态污染

### 3. 缓存 / 记忆化问题
- 检查是否读取了过期缓存
- 检查是否缺少缓存失效
- 检查缓存键冲突

### 4. 外部依赖时序
- API响应以非预期顺序到达
- 数据库写后读一致性问题
- 文件系统时序问题

对每个可能的原因，请提供：
- 需要检查的具体文件/函数
- 什么证据能确认或排除这个假设
- 建议添加什么日志/断言来捕捉这个问题
模板 5：性能问题排查
text

请按照排障SOP来排查这个性能问题。

## 问题描述
- **什么东西慢**：[具体操作/页面/接口]
- **有多慢**：[当前耗时 vs 期望耗时，比如"现在要8秒，应该1秒内"]
- **什么时候开始慢的**：[一直很慢 / 最近变慢 / 某次改动后变慢]
- **数据规模**：[涉及多少数据量、多少用户]

## 入口点
[文件/函数/接口端点]

## 分析要求
1. **画出执行路径**，从开始到结束，标注：
   - 每个函数调用及其预估的复杂度
   - 数据库查询（检查N+1查询、缺失索引、全表扫描）
   - 外部API调用（检查串行调用是否可以改并行）
   - 循环迭代（检查不必要的迭代、嵌套循环）
   - 内存分配（检查不必要的拷贝、大对象创建）

2. **按以下清单排查瓶颈**：
   - [ ] N+1数据库查询？
   - [ ] 缺少数据库索引？
   - [ ] 获取了不需要的数据（查全字段、取了用不到的数据）？
   - [ ] 串行操作可以并行？
   - [ ] 重复计算（同一个结果算了多次）？
   - [ ] 缺少缓存？
   - [ ] 数据无限增长（没有分页/限制的列表）？
   - [ ] 异步操作被同步阻塞了？
   - [ ] 过度渲染（前端）？
   - [ ] 包体积/传输数据量过大？

3. **提出优化方案**：
   - 预计提升多少（量化）
   - 权衡取舍（内存换速度、复杂度换性能）
   - 优化前后代码对比
   - 如何验证改善效果
模板 6：类型/编译错误排查
text

请帮我排查这些类型/编译错误。

## 错误信息
[粘贴所有错误信息]

text


## 背景
- 这些错误是在 [什么改动] 之后出现的
- 错误数量：[N个]
- 这些错误是否相互关联：[你的判断]

## 分析要求
1. **对错误进行分组** —— 很多类型错误是从一个源头级联出来的
2. **找到主要错误** —— 修好哪一个错误能消除最多的其他错误
3. **追踪类型链**：
   - 类型在哪里定义的？
   - 类型在哪里使用的？
   - 类型断言/赋值在哪里失败了？
   - 是类型定义有问题，还是使用方有问题？（判断哪边是错的）
4. **检查以下常见原���**：
   - 缺少类型导出
   - 泛型类型参数错误
   - 联合类型没有正确收窄
   - 可空类型没有正确处理
   - 导入路径有误（路径错了、缺少声明文件）
5. **按依赖顺序提出修复** —— 先修类型定义，再修使用方
模板 7：跨模块集成错误排查
text

请以"根因分析师"的角色来排查这个跨模块集成问题。
请同时使用"依赖追踪"技能来分析模块间的依赖关系。

## 问题描述
- **失败的交互**：[描述模块间的失败交互]
- **模块A**：[名称/路径] —— 在这次交互中的职责
- **模块B**：[名称/路径] —— 在这次交互中的职责
- **它们之间的接口**：[API调用 / 共享类型 / 事件 / 消息队列 / 数据库]

## 错误表现
[粘贴错误信息或描述行为不一致的地方]

text


## 分析要求
1. **检查模块间的契约**：
   - 模块A发送/期望什么？
   - 模块B接收/返回什么？
   - 这个契约定义在哪里？（类型、API Schema、文档）

2. **检查契约违规**：
   - 数据格式不匹配（字段名、类型、嵌套结构）
   - 缺少必需字段
   - 多了意外字段
   - 应该有值的地方是 null/undefined
   - 枚举/常量值不一致
   - 编码/序列化差异

3. **检查版本偏差**：
   - 两个模块是否使用了相同版本的共享类型？
   - 是否一个模块更新了但另一个没有同步更新？

4. **在边界处追踪数据**：
   - 模块A发出数据前的最后一行代码
   - 模块B收到数据后的第一行代码
   - 对比两边的数据形状

5. 查找这个接口的所有消费者——同样的Bug可能在其他地方也存在
模板 8：部署后/环境相关问题排查
text

请按照排障SOP排查这个环境相关问题。

## 问题描述
- **现象**：[描述问题]
- **正常的环境**：[在哪些环境下正常，比如"本地开发、测试环境"]
- **异常的环境**：[在哪些环境下出问题，比如"生产环境"]

## 分析要求
系统性排查以下环境差异：

### 1. 配置差异
- [ ] 环境变量：对比正常和异常环境的值
- [ ] 配置文件：检查是否有环境特定的配置覆盖
- [ ] 功能开关：不同环境的开关状态不同？
- [ ] API地址：是否指向了正确的服务？

### 2. 基础设施差异
- [ ] 运行时版本差异（Node/Python/Java版本）
- [ ] 数据库版本/表结构差异
- [ ] 网络配置（CORS、防火墙、DNS）
- [ ] SSL/TLS证书问题
- [ ] 文件系统权限

### 3. 数据差异
- [ ] 异常环境是否有特殊数据？
- [ ] 数据库迁移状态——表结构是否是最新的？
- [ ] 种子数据差异

### 4. 构建差异
- [ ] 两个环境部署的是同一个代码版本吗？
- [ ] 构建时 vs 运行时配置注入
- [ ] Source Map / 调试模式差异
- [ ] 依赖版本差异（lockfile）

### 5. 规模/时序差异
- [ ] 异常环境的并发用户更多？
- [ ] 异常环境的数据量更大？
- [ ] 超时配置不同？

对每个发现的差异，请指明具体的文件/配置所在位置。
二、架构分析类
模板 9：项目架构全景分析
text

请使用"架构分析"技能，对这个项目进行完整的架构分析。

## 分析要求
1. **项目结构**：顶层模块有哪些，各自的职责是什么？
2. **依赖关系图**：模块之间如何依赖？有没有循环依赖？
3. **数据流**：用户的关键操作中数据是如何流动的？
4. **架构模式**：使用了什么架构模式？是否一致地应用了？
5. **健康评估**：痛点在哪里？技术债在哪里？风险在哪里？

## 请先阅读以下文件
- 根目录结构
- 包管理文件（package.json / go.mod / Cargo.toml 等）
- 构建配置
- README / 文档
- 入口文件
- 各模块的 index / barrel 文件

## 输出格式
请产出完整的架构分析报告，包括：
- 模块职责矩阵表
- 文本形式的架构图
- 关键数据流图
- 健康状况评估
- 改进建议（按优先级排序）
模板 10：特定模块深度分析
text

请使用"架构分析"技能，对以下模块进行深度分析。

## 目标模块
- **路径**：[模块目录路径]
- **名称**：[模块名]
- **我的理解**：[你对这个模块的理解，不确定就写"不清楚"]

## 分析要求
1. 读这个模块里的每一个文件（如果太多就先列出文件清单）
2. 梳理模块的内部结构：
   - 公共API（对外导出了什么）
   - 内部组件及其相互关系
   - 模块内的状态管理方式
   - 外部依赖

3. 画出模块主要操作的数据流图
4. 识别：
   - 这个模块的单一职责是什么（如果有多个职责——标记出来）
   - 与其他模块的接口边界
   - 高复杂度区域
   - 测试覆盖不足的区域
   - 死代码/未使用的导出
   - 与代码库其他部分风格不一致的地方

5. 产出一份"维护者指南"——新开发者需要了解这个模块的哪些关键信息？
模板 11：遗留代码理解（代码考古）
text

请以"代码考古师"的角色来帮我理解这段代码。
在我安全修改它之前，我需要完全理解它的前因后果。

## 目标
- **文件**：[文件路径]
- **具体代码区域**：[函数名/类名，或行范围]
- **我想对它做什么**：[你打算做的修改]

## 分析要求
1. 读目标代码以及它交互的所有代码
2. 重建历史：
   - **原始用途**：这段代码最初为什么被写出来？
   - **当前用途**：它现在实际在做什么？
   - **隐含假设**：这段代码对运行环境做了什么假设？
   - **防御性代码**：它在防范什么边界情况？这些情况还存在吗？
   - **幽灵代码**：有没有死代码、残留逻辑、被注释掉的实验？

3. 识别"承重墙"代码——看起来没用但实际至关重要的代码
4. 标记安全修改点 vs 危险修改点
5. 标注所有"此处有龙"的区域并解释原因

## 输出
请产出完整的代码考古报告，特别是：
- 嵌入的假设及其是否仍然有效
- 对维护者的关键提醒
- 看起来可以重构但实际是故意为之的代码警告
模板 12：依赖关系分析
text

请使用"依赖追踪"技能来分析以下代码元素的依赖关系。

## 目标
- **元素**：[函数名 / 类名 / 模块名]
- **文件**：[文件路径]
- **追踪方向**：[双向 / 仅上游 / 仅下游]
- **追踪深度**：[几层，默认2层]

## 原因
[解释为什么需要这个分析，比如"我要修改这个函数，需要知道改了会影响谁"]

## 请分析
1. **下游依赖**（这个元素依赖什么）：
   - 直接导入和函数调用
   - 传递性依赖（我的依赖又依赖什么）
   - 外部服务、数据库、API调用

2. **上游依赖**（谁依赖这个元素）：
   - 直接调用者/导入者
   - 间接消费者（使用了我的调用者的代码）
   - 测试文件、Mock文件、配置文件中的引用

3. 产出文本形式的依赖关系图
4. 重点标出：
   - 循环依赖
   - 共享依赖（菱形模式）
   - 关键路径依赖（这个坏了影响范围有多大）
三、重构类
模板 13：大规模架构重构规划
text

请以"重构架构师"的角色来规划这次重构。

## 重构目标
我想对项目架构进行重构。

**当前痛点**：
1. [痛点1 —— 比如"模块X是一个包含50多个文件的巨型模块"]
2. [痛点2 —— 比如"A和B之间存在循环依赖"]
3. [痛点3 —— 比如"业务逻辑和UI/API层混在一起"]

**目标架构**（如果有想法就写，没有就让AI建议）：
[描述你期望的架构]

**约束条件**：
- 团队能否在重构期间停止功能开发：[能/不能]
- 时间压力：[无 / 中等 / 紧迫]
- 测试覆盖状况：[良好 / 部分覆盖 / 较差]
- 不能动的区域：[列出不能改的模块或功能]

## 要求
1. 首先用"架构分析"技能梳理当前架构
2. 设计目标架构
3. 做差距分析：当前 → 目标
4. 设计分阶段的迁移计划（分成多个"纪元 Epoch"）：
   - 每个纪元独立有价值且可部署
   - 纪元内的每个步骤都小而安全
   - 每个步骤都有回滚方案
5. 包含风险登记表
6. 包含时间估算
7. 标明哪些步骤可以和日常功能开发并行进行

请产出完整的重构计划。
模板 14：模块拆分重构
text

请以"重构架构师"的角色来规划模块拆分。需要时请使用"依赖追踪"技能。

## 拆分请求
- **要拆分的模块**：[路径]
- **当前规模**：[大约多少文件，多少行代码]
- **拆分原因**：[职责太多 / 文件太多 / 难以导航 / 频繁合并冲突]

## 我的拆分想法（如有）
- 新模块A：[名称] —— 包含 [什么内容]
- 新模块B：[名称] —— 包含 [什么内容]
- 留在原模块：[什么内容]
（如果没有想法就写"请分析后建议拆分方案"）

## 分析和执行要求
1. 读模块里的所有文件
2. 识别自然的职责边界：
   - 哪些函数/类紧密协作？
   - 哪些函数/类有独立的依赖图？
   - 自然的接缝在哪里？

3. 设计拆分方案：
   - 定义每个新模块的职责
   - 定义每个新模块的公共接口
   - 识别共享工具（可能需要独立的共享模块）
   - 验证新结构不会产生循环依赖

4. 创建分步执行计划：
   - 第1步：创建新模块结构（带接口的空壳）
   - 第2步：逐步迁移代码（保留 re-export 做向后兼容）
   - 第3步：更新外部消费者使用新的导入路径
   - 第4步：移除 re-export / 向后兼容层
   - 第5步：验证一切正常

5. 每一步都要说明：
   - 需要创建/修改/删除的文件
   - 具体需要做的改动
   - 如何验证这一步的正确性
   - 如何回滚
模板 15：接口/API迁移重构
text

请以"重构架构师"的角色来规划接口迁移。请使用"变更影响分析"技能。

## 迁移请求

**旧接口**：
[粘贴旧的接口/API/函数签名]

text


**新接口**：
[粘贴新的接口/API/函数签名]

text


**迁移原因**：[为什么旧接口需要改变]
**是否破坏性变更**：[是/否 —— 新接口是否改变了契约]

## 分析和执行要求
1. **找到旧接口的所有消费者**：
   - 直接调用者
   - 间接调用者（通过封装层/适配器）
   - 测试代码
   - Mock/Stub 代码
   - 文档引用

2. **设计迁移策略**，选择最安全的方案：

   **方案A：兼容适配器**（最安全）
   - 创建适配器把旧接口映射到新实现
   - 所有现有消费者使用适配器（零改动）
   - 逐个迁移消费者到新接口
   - 全部迁移完后删除适配器

   **方案B：默认参数**（如果是新增参数）
   - 新增的参数带默认值
   - 现有调用者不受影响
   - 逐个更新调用者传入新参数

   **方案C：并行实现**（如果行为变化）
   - 保留旧函数，在旁边创建新函数
   - 逐步迁移调用者
   - 没有调用者时删除旧函数

3. **产出迁移清单**：
   | 消费者 | 文件 | 当前用法 | 需要的改动 | 优先级 | 工作量 |
   |--------|------|----------|-----------|--------|--------|

4. **按风险从低到高排序迁移顺序**
模板 16：代码模式统一
text

请以"重构架构师"的角色来统一代码库中不一致的模式。

## 不一致的模式
代码库中对于 [描述不一致的内容] 有多种写法：

**模式A**（用在 [列出哪些地方]）：
[模式A的代码示例]

text


**模式B**（用在 [列出哪些地方]）：
[模式B的代码示例]

text


**模式C**（用在 [列出哪些地方]，如果有的话）：
[模式C的代码示例]

text


**期望的标准模式**：[A / B / C / 一个新模式 —— 或写"请推荐"]

## 分析和执行要求
1. **找到每种模式的所有出现位置**
2. **分析每种变体**：
   - 为什么当时选择了这种变体？
   - 是否处理了其他变体没有覆盖的边界情况？
   - 是在活跃维护的代码还是遗留代码中？

3. **推荐标准模式**并说明理由：
   - 为什么这个模式最好？
   - 权衡取舍是什么？
   - 是否有需要例外的情况？

4. **创建迁移计划**：
   - 按风险等级分组
   - 先迁移风险最低的
   - 对每个出现位置提供具体的代码改动

5. **创建模板/工具函数**，让未来容易使用标准模式（防止再次不一致）
模板 17：解耦紧耦合模块
text

请以"重构架构师"的角色来规划模块解耦。请使用"依赖追踪"技能。

## 解耦请求
- **模块A**：[路径] —— [职责]
- **模块B**：[路径] —— [职责]

**耦合症状**：
- [比如"A从B导入了15个东西，B从A导入了8个东西"]
- [比如"改A总是要同时改B"]
- [比如"不启动B就无法测试A"]

## 分析和执行要求
1. **梳理所有耦合点**：
   | 方向 | 共享内容 | 类型 | 能否解耦？ |
   |------|---------|------|-----------|
   | A→B | [函数/类型/状态] | [导入/调用/事件] | [能/不能/复杂] |
   | B→A | [函数/类型/状态] | [导入/调用/事件] | [能/不能/复杂] |

2. **识别循环依赖**（如有）及其根源

3. **为每个耦合点设计解耦策略**：
   - **提取共享接口**：创建共享的类型/接口包
   - **依赖注入**：把依赖作为参数传入
   - **事件驱动**：用事件替代直接调用
   - **提取第三方模块**：把共享逻辑迁到新模块
   - **控制反转**：反转依赖方向

4. **创建分步执行计划**：
   - 每步减少一个耦合点
   - 每步都可以独立部署
   - 每步都有验证方法

5. **验证目标状态**：
   - 没有循环依赖
   - 每个模块可以独立测试
   - 每个模块有清晰、最小的公共接口
模板 18：大文件拆分
text

请以"重构架构师"的角色来规划这个大文件的拆分。

## 拆分请求
- **文件**：[路径]
- **当前行数**：[行数]
- **拆分原因**：[难以导航 / 多重职责 / 频繁合并冲突 / IDE变卡]

## 分析和执行要求
1. 读整个文件
2. **编目所有元素**：
   | 元素 | 类型 | 行范围 | 职责 | 依赖什么 | 谁依赖它 |
   |------|------|--------|------|---------|---------|

3. **识别自然分组**：
   - 分组1：[名称] —— 元素：[列表] —— 理由：[为什么它们属于一起]
   - 分组2：[名称] —— 元素：[列表] —— 理由：[为什么它们属于一起]
   - 共享工具：[被多个分组使用的元素]

4. **设计文件结构**：
[原文件名]/
├── index.ts (barrel导出，保持向后兼容)
├── [分组1名].ts (分组1的元素)
├── [分组2名].ts (分组2的元素)
├── types.ts (共享类型)
└── utils.ts (共享工具函数)

text


5. **执行计划**：
- 第1步：创建barrel文件，re-export所有内容（外部无需改动）
- 第2步：把分组1迁到独立文件，从barrel re-export
- 第3步：验证所有导入仍然正常
- 第4步：迁移分组2，重复
- 第5步：逐步更新外部消费者直接导入（可选，可以渐进完成）

6. 每一步代码都必须保持完全可用
四、变更影响分析类
模板 19：变更前影响评估
text

请以"影响评估师"的角色来评估这个变更的影响。请使用"变更影响分析"技能。

## 计划的变更
- **改动内容**：[描述你打算做的改动]
- **计划修改的文件**：[列出文件]
- **变更性质**：[接口变更 / 行为变更 / 数据结构变更 / 配置变更]

## 评估要求
评估这个变更的完整影响：

1. **第一层——确定影响**：什么一定会受影响？
2. **第二层——可能影响**：什么大概率需要更新？
3. **第三层——潜在影响**：什么在边界情况下可能受影响？
4. **横切关注点**：认证、缓存、监控、API、数据库、文档？
5. **测试影响**：哪些测试会失败？哪些测试缺失？

请产出完整的影响评估报告，包含影响范围树形图。

然后建议：
- 我应该按计划进行，还是调整方案？
- 最安全的变更顺序是什么？
- 每步变更后应该验证什么？
模板 20：修改后验证
text

请以"验证代理"的角色来验证我刚做的修改。

## 已做的修改
1. [文件路径] —— [改了什么]
2. [文件路径] —— [改了什么]
3. [文件路径] —— [改了什么]

**修改目的**：[这些修改要实现什么]
**修改类型**：[Bug修复 / 新功能 / 重构 / 性能优化]

## 验证要求
请进行严格的对抗性验证：

1. **完整性**：我是否改了所有需要改的地方？搜索是否有遗漏的引用。
2. **正确性**：新代��是否真的做了我期望的事？检查逻辑、类型、边界情况。
3. **安全性**：这个修改是否可能引入新Bug？检查错误处理、空值安全、竞态条件。
4. **一致性**：这个修改是否遵循了代码库已有的模式和规范？
5. **对抗性测试**：尝试打破它——什么输入或条件会导致失败？

请严格评判。我宁可现在发现问题，也不要到生产环境才发现。

请产出完整的验证报告，每项检查都要有通过/未通过的结论。
五、通用深度分析
模板 21：强制深度分析（加在任何问题前面）
text

## ⚠️ 分析要求（不可跳过）

在回答我的问题之前，你必须：

1. 真正读取你引用的每个文件——不要根据文件名或函数名猜测
2. 追踪完整的调用链——不要在第一层就停下来
3. 至少生成3个假设��然后逐一验证，再确定答案
4. 每个结论都必须附上 文件:行号 的代码证据
5. 说明你的信心水平，以及什么能提高信心
6. 自我检查："我是在修根因还是只在修症状？"
7. 列出你应该检查但还没检查的文件，以及原因

分析深度：至少 L4（跨模块）。

## 我的问题
[在这里写你的实际问题]
模板 22：追问浅层回答���当 Cursor 给出不够深的回答时）
text

你上一次的回答太浅了，请深入分析。

具体来说：

1. 你说了 "[引用AI之前给出的浅层结论]"。证据在哪里？给我看具体代码。

2. 你建议 "[引用AI之前给出的修复方案]"。但这个Bug到底为什么会存在？
   请用5-Why方法追问：
   - Why #1：为什么 [症状] 会发生？→ 因为___。证据：[文件:行号]
   - Why #2：为什么那个原因会发生？→ 因为___。证据：[文件:行号]
   - 继续追问直到找到真正的根因。

3. 你只看了 [AI之前看过的文件]。但是：
   - 谁调用了这段代码？（上游分析）
   - 这段代码调用了什么？（下游分析）
   - 还有什么代码使用了相同的模式？（横向分析）

4. 你的修复是在 [症状位置] 做的。是否应该在更深层的 [根因位置] 修复更好？

不要再给我浅层回答。我需要带证据的完整分析。
模板 23：代码审查
text

请以"验证代理"的角色进行代码审查，标准按照资深工程师的要求。

## 审查请求
- **要审查的文件**：[列出文件，或写"我刚修改的文件"]
- **背景**：[这些代码实现了什么功能/修复了什么Bug]

## 审查维度

### 正确性
- 代码是否完成了预期的功能？
- 所有边界情况都处理了吗？
- 有没有差一错误（off-by-one）、空指针风险、类型不匹配？

### 安全性
- 有注入漏洞吗？
- 有认证/授权绕过吗？
- 有敏感数据泄露吗？

### 性能
- 有不必要的计算或数据库查询吗？
- 有N+1查询模式吗？
- 有无限增长模式吗？

### 可维护性
- 代码可读且自文档化吗？
- 函数/变量命名清晰准确吗？
- 是否遵循了代码库已有的模式？

### 错误处理
- 所有错误情况都处理了吗？
- 错误信息对调试有帮助吗？
- 错误是否正确传播了？

### 测试
- 这段代码可测试吗？
- 应该为这段代码写什么测试？
- 有没有需要特别测试的边界情况？

对每个发现的问题，请标明：
- **严重程度**：🔴 必须修复 / 🟡 建议修复 / 🟢 可选优化
- **位置**：文件:行号
- **问题**：具体是什么问题
- **修复方案**：怎��修
模板 24：安全性审计
text

请使用"架构分析"技能进行安全性审计。

## 审计范围
- **范围**：[特定模块/功能，或"整个项目"]
- **关注点**：[特定安全关注点，或"全面安全审计"]

## 审计维度

### 1. 认证与授权
- 认证是如何实现的？
- 有没有接口/函数绕过了认证检查？
- 权限检查是在正确的层级做的吗？
- Token是否正确验证和过期了？

### 2. 输入验证
- 用户输入从哪里进入系统？
- 所有用户输入都验证和清理了吗？
- 有SQL注入、XSS、命令注入风险吗？
- 文件上传有正确限制吗？

### 3. 数据保护
- 敏感数据在存储和传输时加密了吗？
- 密钥（API Key、密码）是否妥善管理？
- 日志和错误信息中是否暴露了个人信息？
- 数据库查询是否参数化了？

### 4. 访问控制
- 用户能否访问不属于自己的数据？
- 有IDOR（不安全的直接对象引用）漏洞吗？
- 是否遵循了最小权限原则？

### 5. 依赖安全
- 依赖中有已知漏洞吗？
- 依赖是否及时更新了？

对每个发现，请按以下格式输出：
| 严重程度 | 漏洞类型 | 位置 | 攻击场景 | 修复建议 |
|---------|---------|------|---------|---------|
六、组合工作流（端到端流程）
模板 25：完整的 Bug 修复工作流
text

我需要修复一个Bug。请按照结构化的工作流来处理。

## 阶段1：理解（先做这一步，不要急着修复）
请使用"架构分析"技能帮我理解代码库中相关的部分。
这个Bug涉及的区域是：[相关区域/功能]

## 阶段2：调查
请以"根因分析师"的角色，按照排障SOP进行调查。
Bug症状是：[描述]

## 阶段3：影响评估
在修复之前——请以"影响评估师"的角色评估修复方案的影响。
基于阶段2找到的根因，评估修复会影响什么。

## 阶段4：实施修复
现在实施修复，遵循以下规则：
- 修复根因，不是症状
- 做最小改动（最小影响范围）
- 保持现有代码风格和模式
- 处理错误情况和边界情况
- 列出所有修改的文件

## 阶段5：验证
请以"验证代理"的角色验证修复是否正确、完整、安全。

---
请从阶段1开始。不要跳过任何阶段。
Bug描述：[在这里描述Bug]
模板 26：完整的重构工作流
text

我需要对 [目标] 进行重构。请按照结构化的工作流来处理。

## 阶段1：侦察
请使用"架构分析"技能梳理 [目标区域] 的当前架构。
然后以"代码考古师"的角色理解代码为什么是现在这样的。

## 阶段2：规划
请以"重构架构师"的角色，基于阶段1的发现，设计安全的增量重构计划。
我的目标是：
1. [目标1]
2. [目标2]
3. [目标3]
我的约束是：
- [约束1]
- [约束2]

## 阶段3：影响评估
请以"影响评估师"的角色，对阶段2的重构计划中每个步骤评估完整影响。

## 阶段4：逐步执行
每次只执行一个步骤。
每个步骤完成后，以"验证代理"的角色验证这一步的正确性。
在当前步骤验证通过之前，不要进行下一步。

## 阶段5：最终验证
请以"验证代理"的角色验证整个重构是否正确、完整、没有引入回归。

---
请从阶段1开始。每个阶段完成后等待我确认再进入下一阶段。
重构目标：[描述要重构什么]
模板 27：新功能开发前分析
text

我需要添加一个新功能。在写任何代码之前，帮我理解代码库。

## 功能描述
[描述你要添加的功能]

## 阶段1：架构理解
请使用"架构分析"技能分析：
- 这个功能应该放在现有架构的哪里？
- 它需要和哪些现有模块交互？
- 有没有类似的功能可以参考？

## 阶段2：集成点分析
请使用"依赖追踪"技能，对于这个功能需要交互的每个现有模块：
- 模块的公共API是什么？
- 我需要使用哪些数据类型/接口？
- 其他功能是怎么和这个模块集成的？（作为参考）

## 阶段3：设计
基于阶段1和阶段2的分析，请提出：
1. 新功能的文件结构
2. 新功能的公共接口/API
3. 主要操作的数据流图
4. 与现有代码的集成点（具体用哪些函数/类型）
5. 应该写哪些测试

## 阶段4：影响评估
请以"影响评估师"的角色评估：添加这个功能可能影响哪些现有功能？

## 阶段5：实施计划
产出分步实施计划：
1. 先实现什么（类型/接口）
2. 再实现什么（核心逻辑）
3. 然后实现什么（集成）
4. 接着实现什么（测试）
5. 最后实现什么（清理、文档）

---
请从阶段1开始。
七、快速指令卡片
以下短指令可以随时粘贴到对话中，用来纠正 AI 的行为或深化分析。

text

┌──────────────────────────────────────────────────────────────┐
│  💡 快速指令 —— 随时可用                                      │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  "你刚才的分析太浅了，请用5-Why方法带代码证据深入分析。"            │
│                                                              │
│  "请给我展示从 [A] 到 [B] 的完整调用链。"                       │
│                                                              │
│  "在整个代码库中搜索 [函数名] 的所有调用者。"                     │
│                                                              │
│  "如果我修改了 [函数/类型/接口]，什么会坏掉？"                    │
│                                                              │
│  "这是根因还是症���？请用证据证明。"                              │
│                                                              │
│  "你有什么假设是没有验证的？逐一检查。"                           │
│                                                              │
│  "你应该读但还没读哪些文件？现在去读。"                           │
│                                                              │
│  "请生成3个替代假设并排序。"                                    │
│                                                              │
│  "这个修改可能出什么问题？请做对抗性分析。"                        │
│                                                              │
│  "用树形图展示这个修改的影响范围。"                              │
│                                                              │
│  "做这个修改之前，先跑一遍预检清单。"                            │
│                                                              │
│  "写这段代码的开发者当时的意图是什么？追溯设计意图。"               │
│                                                              │
│  "验证一下：我是否遗漏了 [X] 的某个消费者没有更新？"              │
│                                                              │
│  "这段代码缺少哪些测试？列出来。"                               │
│                                                              │
│  "不要假设，去读代码。"                                        │
│                                                              │
│  "你确定只有这一个原因吗？还有没有其他可能的原因？"                │
│                                                              │
│  "请同时检查错误路径，不要只看正常路径。"                         │
│                                                              │
│  "这个修复方案能覆盖所有相关场景吗？列出所有场景逐一验证。"         │
│                                                              │
│  "请不要说'其他文件类似修改'，列出每一个需要改的文件和具体改动。"    │
│                                                              │
└──────────────────────────────────────────────────────────────┘
八、场景速查表
我遇到的情况	使用模板	附加指令
有报错信息的Bug	模板2 运行时报错	—
没报错但结果不对	模板3 逻辑错误	—
Bug偶尔出现	模板4 偶发性Bug	—
页面/接口很慢	模板5 性能问题	—
TypeScript编译报错一堆	模板6 类型错误	—
改了A模块结果B模块坏了	模板7 跨模块集成	—
本地好好的线上有问题	模板8 环境问题	—
刚接手项目要了解架构	模板9 全景分析	—
要深入了解某个模块	模板10 模块深度分析	—
看不懂一段复杂代码	模板11 代码考古	—
改代码前想知道影响范围	模板19 影响评估	—
改完代码想确认没问题	模板20 修改验证	—
要做大的架构重构	模板13 架构重构	结合模板26端到端流程
要拆分大模块	模板14 模块拆分	—
要迁移接口	模板15 接口迁移	—
代码风格不统一要统一	模板16 模式统一	—
两个模块耦合太紧	模板17 解耦	—
单个文件太大要拆分	模板18 文件拆分	—
AI回答太浅想要深入	模板22 追问	配合模板21强制深度分析
要做代码审查	模板23 代码审查	—
要做安全检查	模板24 安全审计	—
完整修一个Bug的流程	模板25 端到端Bug修复	—
完整做一次重构的流程	模板26 端到端重构	—
开始开发新功能前	模板27 新功能分析	—
九、高级技巧：模板组合串联
当遇到特别复杂的问题时，可以把多个模板串联使用：

text

## 复杂问题分析串联模板

我有一个复杂的问题需要系统分析。请按以下顺序推进，每个阶段完成后等我确认。

### 第一步：全局理解
"请使用架构分析技能，帮我理解 [相关区域] 的架构。特别关注 [相关模块] 之间的关系。"

### 第二步：问题定位
"请以根因分析师的角色，用5-Why方法分析这个问题：[问题描述]。至少提出3个假设。"

### 第三步：依赖分析
"请使用依赖追踪技能，追踪 [根因所在函数/模块] 的所有上下游依赖，画出影响范围。"

### 第四步：影响评估
"请以影响评估师的角色，评估修复方案 [描述修复思路] 的完整影响。"

### 第五步：历史理解
"请以代码考古师的角色，帮我理解 [需要修改的代码] 的设计意图和历史背景，确保修改不会破坏原始设计意图。"

### 第六步：实施
"现在请基于以上所有分析，实施最安全的修复方案。列出所有需要修改的文件和具体改动。"

### 第七步：验证
"请以验证代理的角色，对抗性地验证刚才的修改是否正确、完整、安全。"

---
请从第一步开始。问题是：[你的问题描述]





