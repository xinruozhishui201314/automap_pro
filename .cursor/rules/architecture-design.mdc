---
description: 软件架构设计规则，规避功能缺陷、不稳定、卡住与阻塞
alwaysApply: true
---

# 软件架构设计规则

在设计与评审代码时，从架构层面遵守以下原则，减少功能缺陷、不稳定、卡住和阻塞。

## 1. 并发与阻塞（避免卡住、死锁）

- **锁顺序一致**：全项目约定同一锁层级（如：全局状态 → 子模块 → 队列），多处加锁时严格按同一顺序，避免 AB/BA 死锁。
- **持锁时间最小化**：在锁内只做必要读写与拷贝，不在锁内做 I/O、重计算、等待条件变量（除非设计上就是“等条件”的线程）。
- **避免在回调/服务里等异步结果**：不在 ROS 回调、service handler 内 `future.wait()` 或阻塞等待另一线程；用队列/事件驱动把“请求”交给 worker，结果通过回调或 topic 返回。
- **超时与可取消**：所有“等待”（条件变量、join、wait_for）必须带合理超时或可取消路径，避免永久阻塞。
- **单一生产者/消费者**：有界队列优先 SPSC/MPSC 无锁或细粒度锁；多生产者时明确队列边界与背压策略，防止无界增长导致内存与延迟问题。

## 2. 状态与生命周期（避免不稳定）

- **明确状态机**：有“状态”的模块（如建图/优化/对齐）必须显式状态机；状态转换仅限明确定义的表，禁止未定义转换。
- **初始化与关闭顺序**：依赖关系明确的启动/关闭顺序（如先停数据源，再停消费者，最后释放资源），在文档或 init/shutdown 中写清。
- **线程与资源归属**：每个后台线程由单一模块拥有并负责 join；避免“全局线程”无主释放或重复 join。
- **单写多读**：共享可变状态尽量单写多读，用 atomic 或定期快照；若必须多写，用明确并发模型（如消息传递）而非随意加锁。

## 3. 错误处理与不变式（避免功能缺陷）

- **不吞异常/错误码**：捕获异常或检查返回值时，必须记录并向上传递或转换为明确语义；禁止空 catch 或忽略返回值。
- **不变式前置检查**：在状态转换、关键 API 入口检查前置条件（如非空、已初始化、状态允许），不满足时快速失败并打日志。
- **资源与 RAII**：文件句柄、网络、锁用 RAII 或作用域守卫管理，避免异常路径泄漏。
- **服务/接口契约**：对外的 service/API 明确：前置条件、后置条件、失败语义（可重试 / 不可重试 / 需重置状态）。

## 4. 数据流与边界（避免逻辑错误与阻塞传播）

- **数据流单向**：主数据流方向清晰（如 sensor → frontend → backend → output），避免反向依赖或循环依赖导致级联阻塞。
- **队列与背压**：有界队列满时的策略明确：丢弃最老、拒绝新数据、或阻塞生产者并带超时；在配置或注释中写明。
- **时间戳与对齐**：多源数据按时间戳对齐时，约定对齐策略（最近邻/插值/等待）和最大等待时间，避免“等齐”导致无限等待。
- **大对象与拷贝**：大点云/图像尽量用 shared_ptr 或零拷贝传递；跨线程时明确所有权转移或只读引用，避免无意的大拷贝导致卡顿。

## 5. 评审检查清单（设计/PR 时自检）

- [ ] 是否有在回调或 service 内阻塞等待其他线程？
- [ ] 多锁是否按固定顺序获取？持锁内是否无重计算/I/O？
- [ ] 状态转换是否都有定义？init/shutdown 顺序是否文档化？
- [ ] 错误与异常是否都被记录并处理？资源是否 RAII？
- [ ] 队列是否有界？背压与超时策略是否明确？
- [ ] 数据流是否单向、时间戳对齐是否有最大等待？
